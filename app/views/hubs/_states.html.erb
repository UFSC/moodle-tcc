<div style="margin-top: 15px;">
  <%= semantic_form_for @hub, :url => update_state_hubs_path(position: @hub.position, moodle_user: @tcc.moodle_user) do |h| %>
      <div style="margin-left: 10px;" class="pull-left"><%= h.input :new_state, :label => false, :as => :select, :collection => @hub.class.new_states_collection %></div>
      <span style="margin-left: 10px;"><%= h.action :submit, :label => 'Alterar estado' %></span>
  <% end %>
  <h4><%= "Nota: #{@hub.grade}" if @hub.admin_evaluation_ok? %></h4>
</div>
