<div id="<%= internal_institution.id %>" class="modal hide fade" role="dialog" aria-hidden="true">

  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h3> <%= t(:edit_internal_institution)%> </h3>
  </div>

  <div class="modal-body">
    <%= semantic_form_for internal_institution, url: edit_internal_institution_path(internal_institution, moodle_user: params[:moodle_user]) do |t| %>
      <p><%= t.input :institution_name %></p>

      <div class="modal-footer">
        <button class="btn" data-dismiss="modal" aria-hidden="true">Fechar</button>
        <%= submit_tag t(:save), class: 'btn btn-success' %>
      </div>
    <% end %>
  </div>

</div>