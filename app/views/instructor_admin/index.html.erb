<h3>Lista de TCCs</h3><br>

<table class="table">
  <thead>
        <th>ID ( provisório )</th>
        <th>Nota</th>
        <% TCC_CONFIG["hubs"].size.times do |i| %>
            <th>
              Eixo <%= i+1 %>
            </th>
        <% end %>

  </thead>
  <tbody>
      <% @tccs.each do |tcc| %>
          <tr>
            <td><%= tcc.moodle_user %></td>
            <td>falta..</td>
            <% TCC_CONFIG["hubs"].size.times do |i| %>
                <td>Eixo <%= i+1 %></td>
            <% end %>
          </tr>
      <% end %>
  </tbody>
</table>

<%= will_paginate @tccs %>