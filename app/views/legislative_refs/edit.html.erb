<%= render 'common/tabs_header' %>

<fieldset>
  <legend>Referência do Legislativo <%= " - #{@tcc.student.name}" if policy(@tcc).show_student? %>
    </br>
    <%= @legislative_ref.jurisdiction_or_header %>
  </legend>
  <%= render :partial => 'form' %>
</fieldset>
