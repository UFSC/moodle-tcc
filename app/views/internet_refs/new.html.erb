<%= render 'common/tabs_header' %>

<fieldset>
  <legend>Nova Referência da Internet <%= " - #{@tcc.student.name}" if policy(@tcc).show_student? %></legend>
  <%= render :partial => 'form' %>
</fieldset>