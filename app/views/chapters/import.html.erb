<div class="col-sm-12">
  <%= render 'common/tabs_header' %>
  <% if @chapter.empty? %>

    <% if policy(@tcc).show_student? %>
      <div class="page-header">
        <h2><%= @chapter.chapter_definition.title %>
          <small><%= @tcc.student.name %></small>
        </h2>
      </div>
    <% end %>

    <% if @can_import %>
      <div class="jumbotron">
        <h2>Não existe conteúdo ainda neste capítulo</h2>

        <h4>Para iniciar este capítulo, você deve importar o texto realizado na atividade do Moodle:</h4>
        <br/>
        <%= form_tag execute_import_chapters_path(moodle_user: @tcc.student.moodle_id) do %>
          <%= submit_tag 'Importar texto da atividade do Moodle', class: 'btn btn-large btn-primary' %>
        <% end %>

      </div>
    <% else %>
      <div class="jumbotron">
        <h2>Não existe conteúdo ainda neste capítulo</h2>

        <h3>Você deve concluir primeiro a atividade no Moodle. </h3>
        <h4>
          Somente depois de receber a nota, na atividade no Moodle, você poderá iniciar a construção deste capítulo.
        </h4>
      </div>
    <% end %>
  <% end %>
</div>