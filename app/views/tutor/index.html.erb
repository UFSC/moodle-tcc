<h3>Lista de Portfólios</h3><br />
<h4>
  Tela de Tutor para <%= @group_name %>
</h4>

<%= link_to '#', class: 'btn', id: 'refresh-list', title: 'Atualizar lista' do %>
    <%= display_icon('icon-refresh') %> Atualizar lista
<% end %>

<table class="table table-bordered" id="hubs-table">
  <thead>
  <tr class="blue-header-table">
    <th rowspan="2">Estudante</th>
    <% @hubs.each do |hub_name| %>
        <th colspan="2"><%= hub_name %></th>
    <% end %>
  </tr>
  <tr class="blue-header-table">
    <% @hubs.each do |hub_name| %>
        <th class="status">Estado</th>
        <th class="grades">Nota</th>
    <% end %>
  </tr>
  </thead>
  <tbody>
  <% @tccs.each do |tcc| %>
      <tr>
        <td><%= tcc.name %></td>
        <% tcc.fetch_all_hubs.each do |hub| %>
            <td class="status">
              <%= get_status_label(hub, tcc.moodle_user) %>
            </td>
            <td class="grades">
              <%= hub.grade if hub.admin_evaluation_ok? %>
            </td>
        <% end %>
      </tr>
  <% end %>
  </tbody>

</table>
<%= will_paginate @tccs %>
