<% if policy(Tcc).show_config? %>
  <%= render 'instructor_admin/navbar' %>
<% end %>

<fieldset>
  <legend><%= "#{@modal_title}: " %> <!-- #if policy(@tcc).show_student? %>-->
    </br>
    <%= @tcc_definition.internal_name %>
  </legend>
  <%= render :partial => 'form' %>
</fieldset>