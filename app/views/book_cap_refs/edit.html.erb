<%= render 'common/tabs_header' %>
<fieldset>
  <legend>Referência de Capítulo de Livro <%= " - #{@tcc.student.name}" if policy(@tcc).show_student? %>
    </br>
    <%= @book_cap_ref.cap_title %>
  </legend>
  <%= render :partial => 'form' %>
</fieldset>
